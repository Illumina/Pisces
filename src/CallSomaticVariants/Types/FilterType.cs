namespace CallSomaticVariants.Types
{
    public enum FilterType
    {
        StrandBias,
        LowQscore,
        LowDepth,
        // LowFrequency we apparently dont actually do this in pisces
    }
}
